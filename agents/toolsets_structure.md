# zspace_toolsets Agent Notes

Use the `zspace-core` skill for zSpace API rules. New toolsets should follow the modern `include/zspace/...` and `src/...` structure used by `zspace_core`.

The first v3 toolset is `zTs3DP` under `fabrication`. Treat `legacy/pre-v3` as reference material only.
